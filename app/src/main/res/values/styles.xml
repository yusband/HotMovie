<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.launcher" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">#000000</item>
        <item name="windowBackground">@drawable/splash</item>

    </style>
    <style name="ToolBarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>

    </style>
    <style name="myRatingBar" parent="@android:style/Widget.Holo.RatingBar.Small">
        <item name="android:numStars">5</item>
        <item name="android:stepSize">0.1</item>
        <item name="android:colorControlNormal">#000000</item>
        <item name="android:colorControlActivated">#ffea00</item>
        <item name="android:colorControlHighlight">#c61010</item>
        <!--<item name="android:background">@drawable/ic_star_white_24dp</item>-->
        <!--<item name="android:progress">@drawable/ic_star_yellow_a700_24dp</item>-->
        <item name="android:isIndicator">true</item>
    </style>
    <style name="splashScreenTheme" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
</resources>
